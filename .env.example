ADMIN_TOKEN=change-me
INGEST_TOKEN=change-me-ingest
JWT_SECRET=please-change-this-secret-at-least-32-chars
BOOTSTRAP_ADMIN_EMAIL=admin@bagre.local
BOOTSTRAP_ADMIN_PASSWORD=admin123

# Cloud sync — chave AES-256-GCM para criptografar credenciais de CloudAccount no DB.
# Gerar com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Obrigatória se você for usar /api/cloud-accounts. 64 chars hex.
CLOUD_CREDS_KEY=
